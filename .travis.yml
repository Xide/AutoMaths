language: python
python:
  - "3.6"
install: "pip install -r docs_requirements.txt"

jobs:
  include:
    - stage: test
      script: ./tests/test_docs.sh
    - stage: deploy
      script: mkdocs gh-deploy
      on:
        branch: master
